- hosts: vagrant
  sudo: true
  tasks:
    - name: yum update
      sudo: true
      yum: name=* state=latest
    - name: install basic packages
      yum: name={{ item }} state=installed
      with_items:
        - git
        - curl
        - wget
        - gcc
        - vim
